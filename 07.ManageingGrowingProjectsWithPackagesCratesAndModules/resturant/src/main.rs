// Large lists with external packages
// same module with subpath
// will make the list shorter
use std::io::{self, Write};

// use std::collections::HashMap;

// Glob Operator '*'
// if we want to bring in _all_ public items
use std::collections::*;

fn main() {
    let mut map = HashMap::new();
    map.insert(1, 2);
}
