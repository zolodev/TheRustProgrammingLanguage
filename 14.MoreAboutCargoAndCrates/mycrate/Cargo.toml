[package]
name = "mycrate"
version = "0.1.0"
edition = "2021"
description = "A small package with a lot of potential"
license = "MIT"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
